# Ajedrez

Login:

Logueo:
1. intenta usuario inexistente -> error no existe usuario
3. intenta usario existente + contrase;a no valida -> error contrase;a no valida  
4. intenta usuario + pass correcta -> logueo exitoso.

Registro:
1. intenta usuario existente -> error ya existe usuario
2. intenta usario inexistente -> guardar con cualquier contrase;a   (puede ser vacia)


